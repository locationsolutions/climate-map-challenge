import React from 'react';
import styled from "styled-components";

const popupContent = {
  height: "400px",
  width: "500px",
  marginTop: "20px"
};

const popupHead = {
  fontWeight: "bold",
  fontSize: "16px"

};

const popupText = {
  fontSize: "15px",
  marginBottom: "20px"
};

const okText = {
  fontSize: "15px",
  textAlign: "center"

};

const loadingPopup = {
  fontWeight: "bold",
  fontSize: "16px",
  color: "red"

}

export { popupContent, popupHead, popupText, okText, loadingPopup };
